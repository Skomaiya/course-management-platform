// const express = require('express');
// const router = express.Router();
// const managerController = require('../controllers/managerController');
// const { protect } = require('../middleware/authentication');
// const { manager } = require('../middleware/authorization');

// // Create a new manager
// router.post('/', protect, manager, managerController.createManager);

// // Get all managers
// router.get('/', protect, manager, managerController.getAllManagers);

// // Get a manager by ID
// router.get('/:id', protect, manager, managerController.getManagerById);

// // Update a manager
// router.put('/:id', protect, manager, managerController.updateManager);

// // Delete a manager
// router.delete('/:id', protect, manager, managerController.deleteManager);

// module.exports = router;
